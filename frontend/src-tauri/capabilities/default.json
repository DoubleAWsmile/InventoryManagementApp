{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Only core APIs and the inventory backend configuration command are exposed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

